{
  "name": "@iris/cli",
  "version": "0.0.1",
  "description": "Iris CLI — trigger and monitor test runs from CI/CD pipelines",
  "bin": {
    "iris": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@iris/typescript-config": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "5.9.3"
  }
}
